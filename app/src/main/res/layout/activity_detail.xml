<?xml version="1.0" encoding="utf-8"?>

<!-- Layout til detaljeskærmen for en begivenhed -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="16dp"
    android:paddingTop="80dp">

    <!-- Viser navnet på begivenheden -->
    <TextView
        android:id="@+id/detailNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Navn"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Viser dato -->
    <TextView
        android:id="@+id/detailDateTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dato"
        android:paddingTop="10dp" />

    <!-- Viser tidspunkt -->
    <TextView
        android:id="@+id/detailTimeTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tid"
        android:paddingBottom="10dp" />

    <!-- Viser den fulde beskrivelse af begivenheden -->
    <TextView
        android:id="@+id/detailDescriptionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Beskrivelse"
        android:paddingBottom="20dp" />

    <!-- Knap der åbner begivenhedens link i browseren -->
    <Button
        android:id="@+id/openBrowserButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Åbn i browser" />

    <!-- Knap der lukker detaljeskærmen og går tilbage -->
    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tilbage" />

</LinearLayout>